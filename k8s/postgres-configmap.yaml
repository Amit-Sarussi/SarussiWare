apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  namespace: sarussiware
data:
  init.sql: |
    CREATE TABLE users (
      id SERIAL PRIMARY KEY,
      name TEXT NOT NULL,
      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    );
